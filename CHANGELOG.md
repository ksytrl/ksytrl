# 更新记录

版本号显示在「阅读设置」面板底部，可据此确认自己用的是哪一版。
上线文件（可直接双击使用的单文件版）：[`dist/novel-reader.html`](dist/novel-reader.html)。

## 1.10.0

- **书里的图片、视频终于能看了**。之前导入流水线只处理文字，EPUB 里的 `<img>`、
  `<video>`、SVG `<image>` 会随标签一起被剥掉，PDF 也只取了文字层——图片在导入时
  就丢了。现在：
  - EPUB：插图、SVG 封面页、`<video>`（含 `<source>`）、`<audio>` 与 data: URI 图片
    都会从 ZIP 里读出来，同一张图多处引用只存一份，`alt` 作为图注显示
  - PDF：用 pdf.js 的操作符列表跟踪变换矩阵，算出每张图在页面上的位置，裁出来后
    按纵坐标插回两段文字之间；整页背景、装饰小图会被过滤
  - 阅读时原位显示，图片可点开看大图，视频 / 音频带播放控件；朗读会跳过它们
  - 媒体本体存进 IndexedDB（DB 版本 4），删书时一并清理，完整备份里一起带上
    （单个超过 30MB 的视频不放进备份）
  - 导入预览会提示"另含 N 张图片、N 段视频"，导出 TXT 时标记换成「［图片］」
- 修复：从完整备份**恢复一本书架上没有的书时，封面没有写回**（1.8.0 引入），
  之前会被悄悄重新生成成按书名的配色封面。
- 注意：1.10.0 之前导入的书里本来就没存图片，需要重新导入一次才能看到。

## 1.9.0

- **大书库整理**：书架左侧加了分类侧栏（阅读状态：全部 / 在读 / 未读 / 读完，
  以及全部分类与各分类及数量），右侧默认是**封面墙**，也可切回列表视图。
- **分页**：封面墙每页 60 本、列表每页 24 本，几百本也不会卡。
- **批量管理**：多选书籍后可一次改分类或删除，支持全选本页 / 取消选择。
- **智能整理**：一键按 **格式** / **阅读状态** / **书名系列**（「斗破苍穹 第一部」
  「第二部」会归到一起）重新分类，应用前给出分类数量与示例预览。
- 封面改为按当前页**按需加载**，不再一次性把所有封面读进内存。

## 1.8.0

- **书架封面**：EPUB 取书里自带的内封，PDF 用 pdf.js 渲染首页，其余格式按书名
  生成配色封面（色相由书名决定，竖排书名 + 书脊质感）；鼠标移到封面上可换图
  或重新生成；封面存进 IndexedDB，完整备份里一并带上。
- 封面不进「轻量备份」，保证轻量备份依旧只有几 KB。

## 1.7.0

- **朗读听书**：逐句朗读、读到哪句高亮哪句、一章读完自动接下一章（空的卷标题
  自动跳过）；播放条支持暂停/继续、上一句/下一句、语速 0.75x~2x 与音色选择，
  快捷键 `P`。用系统语音，不联网。

## 1.6.0

- **扫描版 PDF 的 OCR**：pdf.js 渲染每页 + tesseract.js 识别，支持简体/繁体/英文、
  三档清晰度、页码范围、进度与随时停止；全部在本机运行。简体中文语言包随仓库
  自带可离线使用。

## 1.5.0

- **整库备份与恢复**：完整备份（含正文）与轻量备份（只有进度与笔记）；恢复时
  按书识别，已在书架上的只合并进度与书签笔记，不产生重复。
- **「上次读到这里」**：提示条写明章节与进度，正文里画分隔线，可一键回到本章开头。

## 1.4.0

- 导入改为 **PDF / EPUB / TXT / 其他格式** 四栏，每栏只认自己的类型。
- 接入 **pdf.js** 与 **JSZip**（vendored），并新增 MOBI / AZW3 / DOCX / HTML /
  Markdown / FB2 / RTF 的读取。

## 1.3.0

- 修复含「对象流(ObjStm)」的 PDF 直接报错（现代 PDF 普遍使用，等于这类全打不开）。
- 修复带命名空间前缀（`<opf:item>`、`<ncx:navPoint>`）的 EPUB 解析出 0 章。
- 导入失败改为弹窗说明原因并给排查建议；设置面板显示版本号。

## 1.2.0

- **书架分组与搜索**：分类筛选、排序、搜索，导入文件夹时按子目录自动建分类。
- **书签与笔记**：任意位置加书签，选中正文写笔记并高亮，可导出 Markdown。
- **PWA**：可安装到桌面 / 主屏，断网也能打开继续读。

## 1.1.0

- **补回漏掉的章**：按章节号连号校验，把换了写法、被并进上一章的章节切出来。
- **上下无缝滚动阅读**（可切回一章一页）。
- **EPUB / PDF 导入**、**一键导入整个文件夹**、**书名自动清洗**。

## 1.0.0

- 首版：TXT 导入，自动识别编码，清除乱码与推广网址，智能排版，
  多规则分章，任意跳章，四套主题与阅读设置，阅读进度记忆。
